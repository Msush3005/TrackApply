# Job Application Tracker — Frontend (Next.js)

This folder contains the Next.js (App Router) frontend skeleton.

Commands:
- Install: `npm install` (run inside `/frontend`)
- Dev: `npm run dev` (localhost:3000)

Notes:
- Tailwind config and global styles are included.
- No business logic added yet — UI skeleton only.
